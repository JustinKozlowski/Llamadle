{
  "name": "llamadle-api",
  "version": "1.0.0",
  "private": true,
  "description": "Tiny backend for the Llamadle Claude Code plugin: serves the daily puzzle (server-side only, so future puzzles can't be spoiled) and proxies Anthropic's free count_tokens endpoint.",
  "main": "index.js",
  "scripts": {
    "start": "node index.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "express": "^4.19.2",
    "express-rate-limit": "^7.4.0"
  }
}
