{
  "name": "llamadle-api",
  "version": "1.0.0",
  "private": true,
  "description": "Backend for the Llamadle game: serves the daily puzzle (server-side only, so future puzzles can't be spoiled), proxies Anthropic's free count_tokens endpoint, and runs the judge/opponent Gemini calls behind session- and rate-limit-based abuse defenses.",
  "main": "index.js",
  "scripts": {
    "start": "node index.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "cookie-parser": "^1.4.7",
    "express": "^4.19.2",
    "express-rate-limit": "^7.4.0"
  }
}
